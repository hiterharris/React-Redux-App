const initialState = {
    isLoading: false,
    businessList: [],
    error: ""
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
